<UserControl x:Class="NeopilotVS.EnterTokenDialogWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             xmlns:platformUI="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
             xmlns:toolkit="clr-namespace:Community.VisualStudio.Toolkit;assembly=Community.VisualStudio.Toolkit"

             toolkit:Themes.UseVsTheme="True"
             mc:Ignorable="d"
             d:DesignWidth="400"
             d:DesignHeight="120"

             Name="EnterTokenDialogWindow">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" x:Name="lblHeadline"
               Margin="0,0,0,10"
               FontSize="14"
               FontWeight="SemiBold"
               HorizontalAlignment="Left"
               Content="Enter your authentication token:"/>

        <TextBox Grid.Row="1" x:Name="authTokenInput"
                 Height="28"
                 VerticalContentAlignment="Center"
                 Padding="5"/>

        <TextBlock Grid.Row="2" Margin="0,8,0,0" FontSize="12">
            <Hyperlink RequestNavigate="HelpLinkClicked"
                       NavigateUri="https://neopilot-ai.github.io/"
                       TextDecorations="None">
                <Run Text="Get your authentication token"/>
            </Hyperlink>
        </TextBlock>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="OK"
                    Click="BtnOKClicked"
                    Width="80"
                    Height="28"
                    Margin="0,0,10,0"
                    IsDefault="True"
                    Name="button1"/>

            <Button Content="Cancel"
                    Click="BtnCancelClicked"
                    Width="80"
                    Height="28"
                    IsCancel="True"
                    Name="button2"/>
        </StackPanel>
    </Grid>
</UserControl>
